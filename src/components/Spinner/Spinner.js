import React from 'react'
import { LoadingSpinner } from './Spinner.styled'

const Spinner = () => {

    return (
        <LoadingSpinner>
            
        </LoadingSpinner>
    )
}

export default Spinner

